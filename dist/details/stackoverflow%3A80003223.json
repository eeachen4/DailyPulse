{
  "schemaVersion": 2,
  "id": "stackoverflow:80003223",
  "sourceItemId": "80003223",
  "fetchedAt": "2026-09-17T01:45:34.708Z",
  "sourceUrl": "https://stackoverflow.com/questions/80003223/error-when-trying-to-loop-through-and-display-sqlite-table-within-html-page",
  "detail": {
    "longDescription": "I'm currently trying to loop through a table from my SQLite database and display it on my Flask web app however none of the data is showing on the HTML page and when trying to query the table I'm getting the following message within my IDE: (class) Bank_Time The base class of the .SQLAlchemy.Model declarative model class. To define models, subclass db.Model <.SQLAlchemy.Model>, not this. To customize db.Model, subclass this and pass it as model_class to .SQLAlchemy. To customize db.Model at the metaclass level, pass an already created declarative model class as model_class. My code is as follows: admin.html {% extends \"base.html\" %} {% block title %}Admin Panel{% endblock %}{% block content %} <h1 align=\"center\">Admin Panel</h1> <br /> <form method=\"get\" style=\"width: 100%\"> <table style=\"width: 100%\"> <tr> <th>Name</th> <th>Start Time</th> <th>End Time</th> <th>Total Hours</th> <th>Justification</th> <th>Approve/Reject</th> </tr> <tr> <td>hello</td> <td>hello</td> <td>hello</td> <td>hello</td> <td>hello</td> <td><button>Approve</button><button>Reject</button></td> </tr> {% for row in Bank_Time %} <tr> <td>{{Bank_Time.id}}</td> <td>{{Bank_Time.start_time}}</td> <td>{{Bank_Time.end_time}}</td> <td>{{Bank_Time.total_hours}}</td> <td>{{Bank_Time.justification}}</td> <td> <button type=\"button\" class=\"btn btn-primary\">Approve</button ><button type=\"button\" class=\"btn btn-primary\">Reject</button> </td> </tr> {% endfor %} </table> </form> {% endblock %} models.py from . import db from flask_login import UserMixin from sqlalchemy.sql import func from flask_admin import Admin from flask_admin.contrib.sqla import ModelView class Note(db.Model): id = db.Column(db.Integer, primary_key=True) data = db.Column(db.String(10000)) date = db.Column(db.DateTime(timezone=True), default=func.now()) user_id = db.Column(db.Integer, db.ForeignKey('user.id')) class User(db.Model, UserMixin): id = db.Column(db.Integer, primary_key=True) email = db.Column(db.String(150), unique=True) password = db.Column(db.String(150)) first_name = db.Column(db.String(150)) banked_hours = db.Column(db.Float) admin = db.Column(db.Boolean) notes = db.relationship('Note') bank_time = db.relationship('Bank_Time') class Bank_Time(db.Model): __tablename__ = \"Bank_Time\" id = db.Column(db.Integer, primary_key=True) user_id = db.Column(db.Integer, db.ForeignKey('user.id')) start_time = db.Column(db.Integer, nullable=False) end_time = db.Column(db.Integer, nullable=False) total_hours = db.Column(db.Float) justification = db.Column(db.String()) auth.py from flask import Blueprint, render_template, request, flash, redirect, url_for from .models import User, Bank_Time from werkzeug.security import generate_password_hash, check_password_hash from .import db from flask_login import login_user, login_required, logout_user, current_user from datetime import date auth = Blueprint('auth', __name__) @auth.route('/login', methods=['GET', 'POST']) def login(): if request.method == 'POST': email = request.form.get('email') password = request.form.get('password') user = User.query.filter_by(email=email).first() if user: if check_password_hash(user.password, password): flash(\"Logged in successfully!\", category='success') login_user(user, remember=True) return redirect(url_for('views.home')) else: flash(\"Incorrect password, try again.\", category='error') else: flash(\"Email does not exist\", category=('error')) return render_template(\"login.html\", user=current_user) @auth.route('/logout') @login_required def logout(): logout_user() return redirect(url_for('auth.login')) @auth.route('/sign_up', methods=['GET', 'POST']) def sign_up(): if request.method == 'POST': email = request.form.get('email') first_name = request.form.get('firstName') password1 = request.form.get('password1') password2 = request.form.get('password2') user = User.query.filter_by(email=email).first() if user: flash(\"Email already exists\", category='error') elif len(email) < 4: flash(\"Email must be greater than 4 characters.\", category='error') elif len(first_name) < 2: flash(\"First name must be greater than 1 characters.\", category='error') elif password1 != password2: flash(\"Your password doesn't match.\", category='error') elif len(password1) < 7: flash(\"Password must contain a minimum of 7 characters.\", category='error') else: new_user = User( email=email, first_name=first_name, password=generate_password_hash(password1, method='scrypt'), admin=False ) db.session.add(new_user) db.session.commit() login_user(new_user, remember=True) flash(\"Account created!\", category='success') return redirect(url_for('views.home')) return render_template(\"sign_up.html\", user=current_user) @auth.route('/bank_time', methods=['GET', 'POST']) @login_required def bank_time(): if request.method == 'POST': # bank_time = request.form.get('start_time') # current_user.banked_hours += float(bank_time) # Retrieve start_time and end_time and declare them as Python variables start_time = request.form.get('start_time') end_time = request.form.get('end_time') justification = request.form.get('justification') start = start_time.replace('T', '-') start = start.replace(':', '-') start = start.split('-') end = end_time.replace('T', '-') end = end.replace(':', '-') end = end.split('-') # Calculate the time difference in hours and minutes hours = (int(end[3])-int(start[3])) minutes = ((float(end[4])-float(start[4]))/60) # Calculate the number of days between both dates using date function from the datetime module start = date(int(start[0]), int(start[1]), int(start[2])) end = date(int(end[0]), int(end[1]), int(end[2])) days = end - start days = str(days) if days == '0:00:00': days = int(0) elif days == '1 day, 0:00:00': days = int(1) else: days = (days).replace(' days, 0:00:00', '') total_hrs = (int(days)*24) + hours + minutes print(days) print(type(days)) print(hours) print(minutes) print(total_hrs) print(type(total_hrs)) bank_time_request = Bank_Time( user_id=current_user.id, start_time=start_time, end_time=end_time, total_hours=total_hrs, justification=justification ) db.session.add(bank_time_request) db.session.commit() return render_template(\"bank_time.html\", user=current_user) @auth.route('/request_leave', methods=['GET']) @login_required def request_leave(): return render_template(\"request_leave.html\", user=current_user) @auth.route('/admin', methods=['GET', 'POST']) @login_required def admin(): if current_user.admin: return render_template(\"admin.html\", user=current_user, query=Bank_Time.query.all()) else: flash(\"You do not have the relevant permissions required to access this page!\", category='error') return redirect(url_for('auth.login')) @auth.route('/my_time', methods=['GET']) @login_required def my_time(): return render_template(\"my_time.html\", user=current_user)",
    "developer": "gh0st_dancing",
    "comments": 1,
    "stats": [
      {
        "label": "分数",
        "value": "2"
      },
      {
        "label": "回答",
        "value": "1"
      },
      {
        "label": "浏览",
        "value": "109"
      },
      {
        "label": "状态",
        "value": "已回答"
      }
    ]
  }
}
