# 依赖
node_modules/

# 本地环境变量（含密钥，切勿提交）
.env
.env.local
.env.*.local

# 日志与系统文件
*.log
.DS_Store

# 说明：data/daily.json 与 dist/ 需要提交到仓库，
# 供 GitHub Actions 定时任务写入、以及 GitHub Pages 静态托管使用，故不加入 .gitignore。
